// components.ts

import { gButton, gButtonGroup, gIcon, gBreadcrumb, gBreadcrumbItem, gTooltip } from '@gene-design/components';
import type { Plugin } from "vue";

export default [gButton, gButtonGroup, gIcon, gBreadcrumb, gBreadcrumbItem, gTooltip] as Plugin[];